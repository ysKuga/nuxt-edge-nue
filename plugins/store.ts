/**
 * このファイルが存在しないと下記のエラーが発生する模様
 *
 * ```bash
 * ERROR  Plugin not found: ${workspaceFolder}\plugins\store
 *
 * at Promise.all.plugins.map (node_modules\@nuxt\builder-edge\dist\builder.js:5959:15)
 * ```
 */
